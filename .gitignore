# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment / secrets
.env
.env.*
!.env.example

# Editor directories and files
.vscode
.idea

# Claude Code local settings (per-machine)
.claude/settings.local.json
# Claude Code private project context (Jira site/cloud ids, etc.)
CLAUDE.local.md
# Per-machine agent instructions (git identity pinned below — never commit)
AGENTS.local.md
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
