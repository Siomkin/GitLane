## 1. Rust — boundary type and chokepoint

- [x] 1.1 Add `src-tauri/src/git/types/error.rs` with `CommandError { kind, message, detail }` and `CommandErrorKind` (camelCase serde, copied from `RepoOpenError` in `git/types/repo.rs:36-56`), re-export from `git/types.rs`; verify `cargo check` and a serde round-trip test
- [x] 1.2 Add `thiserror = "2"` to `src-tauri/Cargo.toml` after checking `docs/tauri-plugin-decisions.md`; verify `cargo tree -i thiserror` shows only the existing transitive copies plus this one
- [x] 1.3 Change `commands/mod.rs::blocking` to return `Result<T, CommandError>` and add a `sync()` twin; both apply `redact` to `message`/`detail`; keep `impl From<String> for CommandError` (`kind: internal`) for migration; verify the registration-parity test in `commands/mod.rs:40` still passes — plus a `boundary()` adapter for the async updater command; the parity test now also fails any `Result` command whose body skips `blocking`/`sync`/`boundary`
- [x] 1.4 Give `RepoOpenError`, `LeaseError` (`git/write/state_lease.rs:44`), `GithubError` (`git/forge/domain.rs:39`), `HttpError` (`git/oauth/http/types.rs:19`), `CaptureError`/`ReaderError` (`git/forge/bounded_output/error.rs`), `SecretError` (`secrets.rs:107`) `thiserror` derives and `From<_> for CommandError`; verify unit tests mapping each to its `kind` — done for `HttpError`, `SecretError`, `CaptureError`/`ReaderError` (derives) and `RepoOpenError`, `GithubError`, `git2::Error` (`From` impls); `GithubError` keeps a hand-written `Display` over `to_ipc_string()` (12 variants, wording already centralised there) and `LeaseError` gets no conversion (it is message-free by design; its callers word the failure and that wording classifies as `staleLease`)

## 2. Rust — classification at the source

- [x] 2.1 Move hook/auth/network/not-found classification into `git/write/cli/finish.rs` using the regexes from `src/lib/gitError.ts:12-35`; port `src/lib/gitError.test.ts` fixtures to `git/write/cli/tests/`; verify each fixture yields the expected `kind` — classifier lives in `git/write/classify.rs` (crate-visible, called from `CommandError::from(String)`) rather than inside `finish.rs`, so the write layer stays `String`-typed internally while the category is still decided in Rust; fixtures ported as Rust tests in that module
- [x] 2.2 Map `index_lock.rs` stranded-lock detection to `kind: indexLock` and lease mismatches to `staleLease`; verify existing `git/write/tests/recovery` and lease tests assert the kind — `index_lock::is_index_lock_error` now delegates to the classifier; stale-lease wording ("Refresh and try again." / "Refresh and preview again." / "changed before this operation") maps to `staleLease`
- [x] 2.3 Route `forge_op` (`commands/github.rs`) through the same adapter so `GithubError` → `forge`/`auth`; verify `blocking_tests` (`github.rs:271`) still passes — `forge::ipc` / `forge::context` / `forge::accounts` return `CommandError` via `From<GithubError>` (variant → kind/code)
- [x] 2.4 Migrate `commands/{repo,branches,commits,conflicts,files,identity,recovery,remotes,staging,status,tags,terminal,worktrees,auth}.rs` signatures to `Result<_, CommandError>` one module per commit; verify `cargo clippy --all-targets --all-features -- -D warnings` and `cargo test` after each — all fourteen modules plus `updater.rs` migrated in one pass (not one commit per module: nothing is committed in this session); `blocking()` accepts any `Into<CommandError>` closure error and a `sync()` twin covers the 24 synchronous commands
- [x] 2.5 Add a test that no `#[tauri::command]` returns `Result<_, String>` (extend `commands/mod.rs` registration test); verify it fails if a `String` signature is reintroduced

## 3. IPC types and TS api

- [x] 3.1 Add `CommandError`/`CommandErrorKind` to `src/lib/api/git/types/error.ts` (facade in `git/types.ts`) and `isCommandError` to `src/lib/api/validate.ts`; wrap `invoke` in `src/lib/api/index.ts` so non-conforming rejections become `kind: "internal"`; verify a unit test for both shapes — `src/lib/api/invoke.ts` (`CommandError` class, `isCommandError`, `toCommandError`, wrapped `invoke`), `git/types/error.ts`, all 16 wrappers import the wrapper; eslint confines raw core `invoke` to that one file; `invoke.test.ts` covers payload/string/Error/junk and rejection conversion
- [x] 3.2 Reduce `src/lib/gitError.ts` to formatting over `kind` + `message`; delete `classifyIndexLockFailure` and the moved regexes; verify `gitError.test.ts` snapshots of user-facing copy are unchanged — `friendlyGitError` formats from `kind`/`code`/`hook`; classifier exports removed; the per-remote copy-selection regexes and host extraction remain as formatting only (spec allows); fixtures in `gitError.test.ts` are Rust-shaped payloads with unchanged expected copy

## 4. UI / store — surface failed reads

- [x] 4.1 Add per-section `unavailable` flags to `useRepo` and a `reportSectionFailure(section)` helper in `src/store/repoRefresh/`; replace `.catch(() => [])`/`null` at `src/store/repoRefreshActions.ts:121-134`, `src/store/repoRefresh/worktreeScope.ts:39`, `src/store/repoWriteActions/shared.ts:309`; verify store tests assert prior data retained + one notification per failed section — `src/store/repoRefresh/sectionFailures.ts` (`settleRead`, `resolveSectionRead`, `planSectionAvailability`, `reportSectionFailure`), `unavailableSections` on `useRepo`; one warning notification per section transition, dismissed on recovery; tests in `sectionFailures.test.ts` and `repoSectionFailures.test.ts`
- [x] 4.2 Switch `src/store/repoConflictActions.ts`, `src/store/repoLifecycleActions.ts`, `src/lib/stashOutcome.ts`, and any other site matching on error text to branch on `kind`; verify `grep -rn '\.includes(' src/store src/lib | grep -i err` returns only formatting sites — `toasts.ts` branches on `kind` (`auth` → Fix authentication, `indexLock` → Remove lock & retry); `repoConflictActions`, `repoLifecycleActions` (`code: "alreadyARepository"`, classified in Rust), `repoMissing` branch on kind/code; remaining `.includes`/`.test` sites are copy formatting (`stashOutcome.ts` classifies *success* output) or non-error string validation
- [x] 4.3 Render the `unavailable` state in the branch navigator stash/worktree sections and the operation banner; verify component tests for the degraded state — `UnavailableRow` in the branch navigator's Worktrees/Stashes sections (`navItems.ts` `Unavailable` item, hidden while filtering, excluded from counts) and `OperationUnavailableBanner` in `App.tsx`; tests `unavailable.test.tsx`, `OperationUnavailableBanner.test.tsx`, `navItems.test.ts`

## 5. Docs and definition of done

- [x] 5.1 Update `docs/rules/architecture-rules.md` (IPC contract: errors are `CommandError`, classification in Rust, redaction at the chokepoint) and `CLAUDE.md`'s IPC section; verify `bun run lint` passes the doc-linked import rules
- [x] 5.2 Run `bunx tsc --noEmit`, `bun run lint`, `bun run test`, `bun run build`, `(cd src-tauri && cargo fmt --all -- --check && cargo clippy --all-targets --all-features -- -D warnings && cargo test)`, `bun run sizes`, `openspec validate unify-error-model --strict`; verify all pass — all passed on 2026-09-03: tsc clean, eslint clean, vitest 289 files / 3490 tests, vite build OK, `cargo fmt --check` OK, clippy `-D warnings` clean (with `result_large_err` allowed at the two boundary modules), `cargo test` 1092 passed / 1 ignored, sizes ratchet OK, `openspec validate --strict` valid
- [x] 5.3 Exercise in `bun run tauri dev`: commit with a failing `pre-commit` hook, push with a revoked token, and unplug the network during fetch; verify each shows the classified copy and no raw token appears — confirmed by the user on 2026-09-03 in `bun run tauri dev`
