// Commit-modal folder module (GL-163): the container plus its List/Tree views
// and the shared include/exclude Checkbox. The barrel keeps `CommitModal` as
// the module's only public export.
export { CommitModal } from "./CommitModal";
