// Folder module for the file-history mode (GL-193): the container plus its
// presentational leaves (RevisionRow, RevisionInspector/InspectorAction,
// Error/Empty states) and the pure fileHistoryModel derivations. Only the
// container is public; the old ./FileHistoryView import path moves here.
export { FileHistoryView } from "./FileHistoryView";
