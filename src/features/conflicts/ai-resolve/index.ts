// Folder module for the AI conflict resolution: the row the workspace renders,
// the per-file run state above it, and the pure prompt/answer model.
export { AiConflictResolve } from "./AiConflictResolve";
export { aiRunState, useAiResolveRuns, type AiRunState } from "./useAiResolveRuns";
export { landProposal } from "./landProposal";
