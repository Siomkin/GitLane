// Folder module for the terminal pane lifecycle (GL-176/GL-177). The public
// surface is the facade hook; the controller and sub-hooks are internal.
export { useTerminalPanes, type TerminalPanes } from "./useTerminalPanes";
